# Local motion workers

Persistent local inference and support scripts for ARDY and Kimodo. Model weights, quantized encoder artifacts, cached text embeddings, and generated outputs are intentionally excluded from Git.

The chronological integration and tuning record is maintained in `../retargetting/MOTION_DRIVE_EXPERIMENTS.md`.
