# Packages physically installed in ardy/.venv. Install after creating the
# environment with --system-site-packages against runtime/python312.
# Install the vendored ./ardy source separately in editable mode with --no-deps.

accelerate==1.14.0
bitsandbytes==0.50.1
einx==0.4.3
hydra-core==1.3.5
setuptools==84.0.0
transformers==5.8.1
vector-quantize-pytorch==1.25.2
wheel==0.48.0
